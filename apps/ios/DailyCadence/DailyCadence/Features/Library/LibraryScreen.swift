import SwiftUI

/// All-time notes browser — Google Keep-style filterable grid.
/// Not yet implemented — placeholder for now.
struct LibraryScreen: View {
    var body: some View {
        PlaceholderScreen(
            title: "Library",
            systemImage: "square.grid.2x2",
            summary: "Every note you've ever written, filterable by type and searchable."
        )
    }
}

#Preview {
    LibraryScreen()
}
