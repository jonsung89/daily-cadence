import SwiftUI

/// Progress + analytics dashboard. Wireframe Screen 7.
/// Not yet implemented — placeholder for now.
struct DashboardScreen: View {
    var body: some View {
        PlaceholderScreen(
            title: "Progress",
            systemImage: "chart.line.uptrend.xyaxis",
            summary: "Exercise progression, macro trends, and sleep patterns — whichever metrics you pin."
        )
    }
}

#Preview {
    DashboardScreen()
}
